<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.like-mapper">
	<!-- type => 1 선수 -->
	<select id="likeCount" resultType="int" parameterType="LikeVO">
		SELECT NVL(COUNT(*),0)
		FROM myLike
		<where>
			type=#{type}
			<if test="type==1">AND player_id=#{player_id}</if>
		</where>
	</select>
	<select id="isLiked" resultType="int" parameterType="LikeVO">
		SELECT NVL(COUNT(*),0)
		FROM myLike
		<where>
			type=#{type}
			<if test="type==1">AND player_id=#{player_id}</if>
			AND member_id=#{member_id}
		</where>
	</select>
	<insert id="likeOn" parameterType="LikeVO">
		INSERT INTO myLike(type,player_id,member_id)
		VALUES(#{type},#{player_id},#{member_id})
	</insert>
	<delete id="likeOff" parameterType="LikeVO">
		DELETE FROM myLike
		<where>
			type=#{type}
			<if test="type==1">AND player_id=#{player_id}</if>
			AND member_id=#{member_id}
		</where>
	</delete>
</mapper>